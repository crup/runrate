import React from "react";
import { Box, Text } from "ink";

export const FooterHelp = () => (
  <Box marginTop={1}>
    <Text dimColor>g global ←/→ window t tokens $ cost r rate h cache a sessions q quit</Text>
  </Box>
);
